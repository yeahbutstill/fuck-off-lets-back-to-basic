package com.zamannow.basic;

public class TestTernaryOperator {
    public static void main(String[] args) {
        int nilai = 80;
        // type nama variable = kodnsi ? true : false
        String ucapan = nilai >= 80 ? "Selamat anda lulus" : "Anda belum lulus, silakan coba lagi tahun depan";
        System.out.println(ucapan);
    }
}
